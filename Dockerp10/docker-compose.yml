version: "3.8"

services:

  frontend:
    container_name: frontend-container
    build: ./frontend
    ports:
      - "4200:4200"
    depends_on:
      - backend

  backend:
    container_name: backend-container
    build: ./backend
    ports:
      - "8080:8080"
    depends_on:
      - mysql

  mysql:
    container_name: mysql-container
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: project10
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: